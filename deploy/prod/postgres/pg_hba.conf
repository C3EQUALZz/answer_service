# First matching line wins.
#
# TYPE  DATABASE  USER  ADDRESS       METHOD

# Unix socket, inside the container only.
local   all       all                 scram-sha-256

# Loopback inside the container.
host    all       all   127.0.0.1/32  scram-sha-256
host    all       all   ::1/128       scram-sha-256

# The compose network. Docker assigns the address, but it is always private.
host    all       all   10.0.0.0/8    scram-sha-256
host    all       all   172.16.0.0/12 scram-sha-256
host    all       all   192.168.0.0/16 scram-sha-256

# Explicit, so a misconfigured network cannot fall through to a default.
host    all       all   0.0.0.0/0     reject
host    all       all   ::/0          reject
